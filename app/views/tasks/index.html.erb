<h1>My tasks</h1>

<ul class="list-group">
  <% @tasks.each do |task| %>
    <li class="list-group-item border-0">
      [<%= task.completed? ? "X" : " " %>] <%= task.title %>
    </li>
  <% end %>
</ul>
